Upload these two files to the GitHub repository root:
1) wrangler.jsonc
2) .assetsignore

This version deploys the repository root but tells Wrangler not to include .git and other non-site files.
It is intended for the current repository structure where index.html, gift-guide.html and assets/ are already at root.
